{
  "name": "rankrush-realtime",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install:fat32": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/install-fat32.ps1",
    "dev": "concurrently -n API,WEB -c magenta,cyan \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "test": "npm run test --prefix apps/api && npm run test --prefix apps/web",
    "seed": "npm run seed --prefix apps/api",
    "seed:library": "npm run seed:library --prefix apps/api",
    "admin:setup": "npm run admin:setup --prefix apps/api",
    "lint": "npm run lint --prefix apps/api && npm run lint --prefix apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.9.5"
  },
  "engines": {
    "node": ">=20"
  }
}
